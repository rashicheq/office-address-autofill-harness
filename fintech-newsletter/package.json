{
  "name": "fintech-newsletter",
  "private": true,
  "version": "0.1.0",
  "description": "Daily fintech/credit-card news brief: per-section Claude web-search fetches, LinkedIn angle + reflection question synthesis, and a Top Cards of the Month generator.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "test:backend": "npm run test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
