{
  "name": "office-address-autofill-harness",
  "private": true,
  "version": "0.1.0",
  "description": "Local test harness for validating the Office Address Autofill V2 ranking/formatting/confidence logic against mock data.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "test:backend": "npm run test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
